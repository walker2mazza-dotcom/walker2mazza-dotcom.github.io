<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>For You 🤍</title>

  <style>
    body {
      margin: 0;
      font-family: 'Georgia', serif;
      background: radial-gradient(circle at top, #1a1a2e, #0f0c29);
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      text-align: center;
      padding: 20px;
      overflow: hidden;
    }

    .card {
      max-width: 520px;
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(10px);
      padding: 35px;
      border-radius: 20px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.4);
      animation: fadeIn 2s ease;
    }

    h1 {
      font-size: 2.4rem;
      margin-bottom: 15px;
      letter-spacing: 1px;
    }

    p {
      font-size: 1.15rem;
      line-height: 1.7;
      margin-bottom: 15px;
    }

    .signature {
      margin-top: 30px;
      font-style: italic;
      opacity: 0.85;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .heart {
      position: absolute;
      font-size: 18px;
      opacity: 0.2;
      animation: float 8s infinite ease-in-out;
    }

    @keyframes float {
      0% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
      100% { transform: translateY(0); }
    }
  </style>
</head>

<body>

  <!-- floating hearts -->
  <div class="heart" style="top:10%; left:15%;">🤍</div>
  <div class="heart" style="top:30%; left:80%;">🤍</div>
  <div class="heart" style="top:70%; left:20%;">🤍</div>

  <!-- music (autoplay, hidden) -->
  <iframe
    width="0"
    height="0"
    src="https://www.youtube.com/embed/uxpDa-c-4Mc?autoplay=1&loop=1&playlist=uxpDa-c-4Mc&controls=0"
    frameborder="0"
    allow="autoplay"
  ></iframe>

  <div class="card">
    <h1>Hey <span style="color:#ffb3c6;">My Love</span> 🤍</h1>

    <p>
      I made this just for you — not because I had to,
      but because you mean that much to me.
    </p>

    <p>
      In every room, every moment, every quiet thought,
      it’s you. Always you.
      You bring peace into my life in a way I can’t explain,
      and loving you feels natural… like home.
    </p>

    <p>
      I’m proud to call you mine,
      and even prouder that I get to love you openly,
      intentionally, and fully.
    </p>

    <div class="signature">
      — Yours, always. <br/>
      ❤️ Wemz
    </div>
  </div>

</body>
</html>
